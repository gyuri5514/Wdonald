<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wdelivery.admin.dao.AdminDAO">

	<select id="indexView" resultType="paymentVO" >
		select order_date,order_status,final_price,payment_type,admin.store_name from order_list, admin
		where admin.store_code = order_list.store_code
	</select>
	
	<select id="userSelect" resultType="userVO">
		select * from user_info
	</select>
	
	<select id="getUserContent" resultType="userVO">
		<![CDATA[
			select * from
			(select row_number() over(order by user_seq) as row,
			* from user_info where row_number()<=20)
			user_info
		]]>
	</select>
		
	<!-- 	<select id="indexView" resultType="paymentVO" >
				select * from order_list order by order_seq 
		</select> -->
	<select id="selectCoupon" resultType="adminCouponVO">
		SELECT * FROM coupon 
	</select>
	
	<select id="couponCheck" resultType="int">
		SELECT count(*) FROM coupon WHERE coupon_code = #{cosupon_code}
	</select>
	
	<insert id="addCoupon">
		INSERT INTO coupon (coupon_code, coupon_title, coupon_type, coupon_regdate, coupon_enddate, coupon_canuse)
		VALUES(#{coupon_code}, #{coupon_title}, #{coupon_type}, #{coupon_regdate}, #{coupon_enddate}, #{coupon_canuse})
	</insert>

</mapper>